# =============================================
# Configuración de Base de Datos MySQL
# =============================================
spring.datasource.url=jdbc:mysql://localhost:3306/AlkeWallet?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=wallet_user
spring.datasource.password=Wuser_1234!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =============================================
# Configuración de JPA / Hibernate (opcional por ahora)
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# Puerto del servidor
server.port=8080

# Permitir que el frontend estático se sirva correctamente
spring.web.resources.static-locations=classpath:/static/